"""Nepse Quant Workstation — PySide6 desktop GUI.

Runs as a parallel interface to the existing TUI. Imports only from
`backend.core.*`; never reaches into `apps/tui/` or `apps/classic/`.

Entry point: python -m apps.desktop
"""
